const initialState = [
  'American Dad',
  'Roger',
  'Chuck'
]

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state
  }
}

export default reducer
